class Position{
    constructor(posId, docId, latitude, longitude){
        this.posId = posId;
        this.docId = docId;
        this.latitude = latitude;
        this.longitude = longitude;
    }
}

export default Position;